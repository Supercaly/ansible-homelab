{{ ansible_managed | comment }}
caddy_version: "{{ caddy_version }}"
xcaddy_version: "{{ __xcaddy_version }}"
go_version: "{{ __go_version }}"
plugins:
{% for plugin in caddy_plugins | sort %}
  - {{ plugin }}
{% endfor %}
