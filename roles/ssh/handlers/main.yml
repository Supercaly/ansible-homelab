---
- name: reload systemd
  ansible.builtin.systemd_service:
    daemon_reload: true
  when: ansible_service_mgr == "systemd"

- name: restart ssh
  ansible.builtin.service:
    name: "{{ ssh_sshd_name }}"
    state: "restarted"
