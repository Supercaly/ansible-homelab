---
#########################################
# Play that creates the LXC on the node.
#########################################
- name: Create ntfy LXC.
  hosts: node
  become: true
  vars_files:
    - "{{ inventory_dir }}/host_vars/ntfy/proxmox.yml"
  roles:
    - role: roles/create-lxc

#############################
# Play that sets up the host.
#############################
- name: Provision ntfy LXC.
  hosts: ntfy
  become: true

  roles:
    - role: roles/system-locale
    - role: roles/system-update
    - role: roles/system-users
    - role: roles/ssh
    - role: roles/motd
    - role: roles/ntfy
